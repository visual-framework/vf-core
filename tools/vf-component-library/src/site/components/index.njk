---
is_index: true
title: Component overview
subtitle: These components are available.
date: 2019-04-09 12:24:50
section: components
tags: sections
templateEngineOverride: njk
layout: layouts/section.njk
hideSubPosts: true
---

{# The Nunjucks templates as well as any CSS, Sass, JS and image are available for use.

## Where's my CSS and JS?

They're generated by the `vf-componenet-rollup`:

- `<link rel="stylesheet" media="all" href="{{ '/css/styles.css' | url }}" />`
- `<script src="{{ '/scripts/scripts.js' | url }}"></script>` #}

{# ## All components #}

<div class="vf-content">


{% render '@vf-search-client-side--default', {
  "search_client_prompt": "Search components and documentation",
  "search_client_button": true,
  "search_client_button_text": "Search",
  "search_client_action": "/search/",
  "search_client_results": false
} %}

{% set types = ['layout', 'element', 'block', 'container', 'utility'] %}


{%- for type in types %}

### {{type}}

<ul>
{%- for item in fractalEnvironment.localComponents %}
  {%- if item.isHidden == false and item.variants.items[0].context['component-type'] == type %}
<li><a class="vf-link" href="{{ item.handle}}">{{ item.title }}</a> <code> {{ item.name }} </code></li>
  {% endif %}
{% endfor -%}
</ul>

{% endfor -%}

</div>




