---
title: Theme colours
date: 2021-06-22 12:24:50
section: designtokens
templateEngineOverride: njk
tags:
  - posts
  - sections
  - designtokens
layout: layouts/base.njk
---

{% set type %}color{% endset %}
<h1 class="vf-intro__heading">{{ title }}</h1>

<div class="vf-content">
<h3>Brand</h3>
</div>
{# <pre>{{ tokens.themeBrand.properties | dump(2 )}}</pre> #}
<section class="vf-grid vf-grid__col-3">
  {% set type %}color{% endset %}
  {% for item in tokens.themeBrand.properties %}
    {% include "./token-card.njk" %}
  {% else %}
    <p>Something went wrong.</p>
  {% endfor %}
</section>

<div class="vf-content">
<h3>Text and interactive links</h3>
</div>
{# <pre>{{ tokens.themeText.properties | dump(2 )}}</pre> #}
<section class="vf-grid vf-grid__col-3">
  {% set type %}color{% endset %}
  {% for item in tokens.themeText.properties %}
    {% include "./token-card.njk" %}
  {% else %}
    <p>Something went wrong.</p>
  {% endfor %}
</section>

<div class="vf-content">
<h3>Buttons</h3>
</div>
{# <pre>{{ tokens.themeButtons.properties | dump(2 )}}</pre> #}
<section class="vf-grid vf-grid__col-3">
  {% set type %}color{% endset %}
  {% for item in tokens.themeButtons.properties %}
    {% include "./token-card.njk" %}
  {% else %}
    <p>Something went wrong.</p>
  {% endfor %}
</section>

<div class="vf-content">
<h3>UI and neutral colours</h3>
</div>
<section class="vf-grid vf-grid__col-3">
  {% for item in tokens.neutral_colours.properties %}
    {% include "./token-card.njk" %}
  {% else %}
    <p>Something went wrong.</p>
  {% endfor %}
  {% for item in tokens.uiColors.properties %}
    {% include "./token-card.njk" %}
  {% else %}
    <p>Something went wrong.</p>
  {% endfor %}
</section>


<div class="vf-content">
<h3>Decorative brand colour palette</h3>
</div>
<section class="vf-grid vf-grid__col-3">
  {% for item in tokens.colors.properties %}
    {% include "./token-card.njk" %}
  {% else %}
    <p>Something went wrong.</p>
  {% endfor %}
</section>
