{% set vfLogo = frctl.components.find('@vf-logo--responsive').render(null, renderEnv, { preview: withLayout, collate: withCollation }) | async(true) %}

<header class="vf-header">
  <div class="vf-global-header">
    <div class="vf-global-header__inner">
    {{ vfLogo }}
    <p class="vf-global-header__site-name">Visual Framework for Life Science websites</p>
    </div>
  </div>
</header>
